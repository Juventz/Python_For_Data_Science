# The file __init__.py is a special file that is executed
# when the package is imported. It can be used to initialize
# the package or to import the modules that are part of the package.
# It shows that the directory is a package and can contain modules.
# The __init__.py file can be empty or
# contain the initialization code for the package.

from .functions import count_in_list
